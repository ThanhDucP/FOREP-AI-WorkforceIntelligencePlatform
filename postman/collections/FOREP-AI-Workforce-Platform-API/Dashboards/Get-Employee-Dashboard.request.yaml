$kind: http-request
name: Get Employee Dashboard
method: GET
url: '{{base_url}}/api/v1/dashboard/employee/{{employee_id}}'
order: 1000
headers:
  - key: Authorization
    value: 'Bearer {{jwt_token}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status 200 OK", function () {
        pm.response.to.have.status(200);
      });
      pm.test("Dashboard has data", function () {
        var json = pm.response.json();
        pm.expect(json.data).to.exist;
      });
