$kind: http-request
name: Approve Leave Request (Admin/Manager)
method: PUT
url: '{{base_url}}/api/v1/leaves/{{leave_id}}/approve'
order: 6080
headers:
  - key: Authorization
    value: 'Bearer {{jwt_token}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status 200 OK", function () {
        pm.response.to.have.status(200);
      });
