$kind: http-request
name: Get Team AI Insights
method: GET
url: '{{base_url}}/api/v1/ai/insights/team/{{team_id}}'
order: 8040
headers:
  - key: Authorization
    value: 'Bearer {{jwt_token}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status 200 OK", function () {
        pm.response.to.have.status(200);
      });
      pm.test("Insights response is array", function () {
        pm.expect(pm.response.json().data).to.be.an("array");
      });
