$kind: http-request
name: Get Managed Team AI Insights
method: GET
url: '{{base_url}}/api/v1/ai/insights/managed-teams'
order: 8050
headers:
  - key: Authorization
    value: 'Bearer {{jwt_token}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status 200 OK", function () {
        pm.response.to.have.status(200);
      });
      pm.test("Insights response is array", function () {
        pm.expect(pm.response.json().data).to.be.an("array");
      });
